I executed the `/challenge/run` command in the terminal and was prompted to navigate to the root directory `/`.  
I then used `cd /` to change the directory.  
After that, I ran `./challenge/run` and successfully obtained the flag.
